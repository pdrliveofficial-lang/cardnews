# 움직이는 웹툰 릴스 (클링 + 나레이션) — 이어서 작업하는 법

> 2026-08-09 실험. 정지 웹툰컷 대신 **클링으로 실제로 움직이는 영상** + **한국어 나레이션**을 얹은 릴스.
> 완성 시안: `output/lab-024/reel-kling.mp4` (빈 축의금 봉투 사건, 29초)

## 제작 공식 (전부 무료, 이미지 크레딧만 소모)

```
사연 1편 = 프레임 5장(10크레딧) + 클링 5클립(무료) + 나레이션 6개(무료) + 조립
```

| 단계 | 도구 | 비용 | 소요 |
|---|---|---|---|
| ① 프레임 5장 | nano_banana_pro (CLI) | 장당 2크레딧 | 2분 |
| ② 클링 클립 5개 | 힉스필드 웹 UI **Kling 2.5 Turbo** | **무료** | 20~40분(큐) |
| ③ 나레이션 6개 | Gemini TTS 또는 Edge TTS | 무료 | 1~7분 |
| ④ 조립 | `make_reel_kling.py <slug>` | — | 1분 |

### ⚠️ 클링 무료(Unlimited) 조건 — 실측
- **스타트 프레임만** 넣을 것. 720p + Unlimited 토글 ON → 버튼이 `Generate Unlimited`로 바뀌면 무료.
- **엔드 프레임을 같이 넣으면 강제로 1080p로 바뀌고 5초당 6크레딧 과금**된다(경고 토스트가 뜸).
  엔드 포즈는 프롬프트로 지시하면 결과 차이가 거의 없다.
- 자동화 시 `Generate` 라벨에 `Unlimited`가 없으면 **중단**하도록 가드를 넣을 것.

### 나레이션 선택
- **Gemini TTS**(`gemini-2.5-flash-preview-tts`)가 품질 최고 — "차분하게 사연 읽는 톤으로" 같은 **연기 지시**가 먹힌다.
  화자 분리: 사연=`Kore`, 인용 대사=`Leda`.
  ⚠️ 무료 한도 `limit: 10` — **재시도도 호출로 카운트**되어 금방 막힌다. 호출 간격 13초 이상, 하루 총량도 넉넉지 않음.
- **Edge TTS**(`narrate_edge.py`)는 무제한·무료. 품질은 한 단계 아래지만 실사용 가능.
  계정이 '제보 전달자'이므로 **나레이터 목소리는 사연 주인공 성별과 무관하게 하나로 고정**(SunHi), 인용 대사만 대비(Hyunsu).
  간헐적으로 빈 응답을 주므로 재시도 필수(이미 구현됨).

## 🚨 2026-08-09 사고 — 클립 15개 유실

**무슨 일**: 프레임 이미지 31장을 `fnf/assets` DELETE로 지웠더니, **그 이미지를 스타트 프레임으로 쓴 클링 클립까지 함께 사라졌다.**
힉스필드가 같은 생성 세션 결과물을 묶어 관리하는 것으로 보인다. 클립 30개 중 15개만 회수하고 나머지는 유실.

**재발 방지**: 힉스필드 삭제는 **해당 사연의 모든 산출물(프레임+클립)을 전부 다운로드한 뒤 마지막에 한 번에** 실행할 것.
클립이 하나라도 생성 중이면 그 사연의 프레임을 지우지 말 것.

**추가 함정**: `higgsfield generate list`는 기본 20건, `--size 100`을 줘도 **삭제된 자산의 잡은 목록에서 사라진다**.
진행률은 목록이 아니라 **로컬에 내려받은 파일 수**로 세야 한다(`progress.py`가 그렇게 되어 있음).

## 지금 상태 (2026-08-09 종료 시점)

6편이 **클립만 부족한 상태**로 멈춰 있다. 프레임·나레이션·사양은 전부 준비됨.

| 사연 | 프레임 | 나레이션 | 보유 클립 | 재생성 필요 |
|---|---|---|---|---|
| case-017 | 5/5 | 6/6 | scene5 | 1,2,3,4 |
| case-018 | 5/5 | 6/6 | scene2 | 1,3,4,5 |
| case-019 | 5/5 | 6/6 | scene2,5 | 1,3,4 |
| lab-011 | 5/5 | 6/6 | scene2,5 | 1,3,4 |
| lab-012 | 5/5 | 6/6 | scene4 | 1,2,3,5 |
| lab-013 | 5/5 | 6/6 | scene1,4 | 2,3,5 |

**이어서 하는 법**
1. `assets/kling_payload.json`에 사연별 5개 클립의 (프레임 URL, 모션 프롬프트)가 그대로 있다.
   ⚠️ 단 프레임 URL은 힉스필드에서 삭제돼 만료됐을 수 있으니, 로컬 `assets/<slug>/frames/0N.png`를 다시 업로드해 쓸 것.
2. 힉스필드 웹 UI에서 위 표의 "재생성 필요" 클립만 생성(스타트 프레임만, 720p, Unlimited).
3. 다운로드 → `assets/<slug>/clips/sceneN.mp4`로 저장.
4. `python make_reel_kling.py <slug>` → `output/<slug>/reel-kling.mp4`
5. 전부 끝난 뒤 힉스필드 자산 일괄 삭제.

## 파일 맵

```
reel_specs.py                     사연별 장면 프롬프트·자막·나레이션 사양 (6편)
assets/kling_payload.json         클립별 (프레임URL, 모션 프롬프트)
assets/<slug>/frames/0N.png       프레임 원본 (로컬 보관)
assets/<slug>/clips/sceneN.mp4    클링 클립
assets/<slug>/narr/narrN.mp3      나레이션 (1~5=장면, 6=판정)
output/<slug>/reel-kling.mp4      완성 릴스
make_reel_kling.py                조립 (자막 2줄 랩핑 + 나레이션 믹스 + BGM)
narrate.py / narrate_edge.py      나레이션 생성 (Gemini / Edge)
clip_reaper.py                    클립 완료 시 다운로드 + 삭제대기 목록 작성
progress.py                       진행률 대시보드
```

⚠️ **자막은 drawtext라 자동 줄바꿈이 없다** — `reel_specs.py`에서 반드시 2줄로 끊어 적을 것(줄당 13자 안팎).
한 줄로 넣으면 720px 프레임을 벗어난다(2026-08-09 실사고).
